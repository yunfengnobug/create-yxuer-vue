---
description: 命名规范
alwaysApply: true
---

# 命名规范

## 目录

有复数结构时采用复数命名，全小写：

`docs`、`assets`、`components`、`directives`、`utils`、`views`

## 文件

| 类型 | 规则 | 示例 |
|---|---|---|
| Vue 文件 | 大驼峰，名词化 | `UserManage.vue`、`BaseTitle.vue` |
| 图像文件 | 全小写，下划线分隔 | `avatar.png`、`default_avatar.png` |
| TS/JS 文件 | 全小写，短横线分隔 | `request.ts`、`day-js.ts` |

## 代码

| 类型 | 规则 | 示例 |
|---|---|---|
| 常量 | 全大写，下划线分隔 | `PI`、`MAX_PAGE_SIZE` |
| 变量 | 小驼峰 | `userList`、`currentMenuId` |
| 方法 | 小驼峰，动宾结构 | `getTableList`、`handleSubmit` |
| 类 | 大驼峰 | `class Calculator extends Abacus {}` |
| 样式类名 | 全小写，短横线分隔 | `user-list`、`user-item` |

## API 与页面的方法命名区分

- **`src/api/` 中**：查询接口用 `find` 前缀 → `findUserList`、`findOrderDetail`
- **`.vue` 中调用接口并赋值**：用 `get` 前缀 → `getUserList`、`getOrderDetail`

```typescript
// src/api/user.ts
export const findUserList = (data: FindUserListParams) =>
  request.get('/user/list', data)

// src/views/UserManage.vue
import { findUserList } from '@/api/user'

const userList = ref([])
const getUserList = async () => {
  const res = await findUserList({ page: 1 })
  userList.value = res.result.list
}
```
