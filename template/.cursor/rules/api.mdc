---
description: API 接口层规范 — 函数风格、类型定义、请求工具与错误处理
globs: "src/api/**/*.ts"
alwaysApply: false
---

# API 接口规范

## 文件组织

按业务模块在 `src/api/` 下拆分文件，通用接口放 `index.ts`。

## 函数风格

使用 `export const` + 箭头函数，配合 JSDoc 注明用途与作者，参数使用 TypeScript 接口定义：

```typescript
import request from '@/utils/request'
const viteCloud = import.meta.env.VITE_CLOUD

interface FindMyChildMenusParams {
  /** 学校编码 */
  schoolCode: string
  /** 菜单ID */
  menuId: string
  /** 额外返回字段，多个用逗号分隔（如 icon,path,component） */
  extraFields?: string
  /** 状态：0-禁用 1-启用 */
  status?: number
}

/**
 * 获取权限子菜单 @author 张三
 */
export const findMyChildMenus = (data: FindMyChildMenusParams) =>
  request.get(`${viteCloud}/base/menu/check/findMyChildMenus`, data, 'query')
```

## 请求工具

统一使用 `@/utils/request`，支持 `get` / `post` / `put` / `delete` 方法。

第三个参数 `paramsType` 控制传参方式：
- `'query'` — URL 查询参数（GET 默认）
- `'body'` — 请求体（POST 默认）
- 使用默认方式时可省略

## 错误处理

响应拦截器已统一处理 toast 提示和会话过期跳转，API 函数一般无需额外处理错误，调用方按需 `try/catch` 即可。
