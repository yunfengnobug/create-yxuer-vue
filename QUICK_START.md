# 快速开始指南

这是一个 5 分钟快速上手指南，帮助您快速使用 `create-yxuer-vue` 创建项目。

## 🚀 第一步：创建项目

选择您喜欢的包管理器：

```bash
# 使用 npm
npm create yxuer-vue@latest my-app

# 使用 pnpm（推荐）
pnpm create yxuer-vue@latest my-app

# 使用 yarn
yarn create yxuer-vue@latest my-app
```

## 📝 第二步：回答配置问题

CLI 会询问您几个问题，按需选择即可：

1. **项目名称**：输入项目名称（默认：yxuer-vue-project）
2. **目录处理**：如果目录已存在，选择如何处理
3. **项目标题**：浏览器标签显示的标题（默认：智慧校园）
4. **应用 ID**：应用标识（默认：YXUER_HOME）
5. **路由模式**：Hash 或 History（推荐 Hash）
6. **富文本编辑器**：是否需要 AIEditor（根据需求选择）
7. **文件上传**：是否需要七牛云上传（根据需求选择）

## 📦 第三步：安装依赖

```bash
cd my-app
pnpm install
```

⏱️ 安装时间约 1-3 分钟，取决于网络速度。

## ⚙️ 第四步：配置环境变量

打开 `.env.devmaster` 文件，修改后端接口地址：

```env
# 后端接口地址
VITE_TARGET_URL=http://your-backend-api.com

# 其他配置保持默认即可
```

💡 **提示**：如果是本地开发，使用 `.env.devlocal` 文件。

## 🎯 第五步：启动项目

```bash
# 连接测试环境
pnpm dev:master

# 或连接本地后端
pnpm dev:local
```

🎉 **成功！** 浏览器会自动打开 `http://localhost:5173`

## 📂 项目结构一览

```
my-app/
├── src/
│   ├── api/              # API 接口
│   ├── components/       # 组件
│   ├── router/           # 路由
│   ├── stores/           # 状态管理
│   ├── utils/            # 工具函数
│   │   ├── request.ts    # Axios 封装
│   │   ├── dayjs.ts      # 日期处理
│   │   └── download.ts   # 文件下载
│   ├── views/            # 页面
│   └── App.vue           # 根组件
├── .env.*                # 环境变量
└── vite.config.ts        # Vite 配置
```

## 🔧 常用命令

```bash
# 开发
pnpm dev:local          # 本地开发
pnpm dev:master         # 测试环境
pnpm dev:tencentCloud   # 腾讯云环境

# 构建
pnpm build:master       # 打包测试环境
pnpm build:tencentCloud # 打包腾讯云

# 代码质量
pnpm lint               # 代码检查
pnpm format             # 代码格式化
pnpm type-check         # 类型检查

# 分析
pnpm report:master      # 打包分析
```

## 💡 下一步

1. **配置路由权限**

   - 在后台配置菜单权限
   - URL 中添加 `menuId` 参数

2. **开发第一个页面**

   - 在 `src/views/` 创建页面组件
   - 在 VSCode 中输入 `vue3` 快速生成模板

3. **调用后端接口**

   - 在 `src/api/` 定义接口
   - 使用 `src/utils/request.ts` 发起请求

4. **添加状态管理**
   - 在 `src/stores/` 创建 store
   - 使用 Pinia 管理状态

## 📚 学习资源

- [Vue 3 文档](https://cn.vuejs.org/)
- [Vite 文档](https://cn.vitejs.dev/)
- [Ant Design Vue 文档](https://antdv.com/)
- [Pinia 文档](https://pinia.vuejs.org/zh/)
- [TypeScript 文档](https://www.typescriptlang.org/zh/)

## ❓ 遇到问题？

1. 查看项目根目录的 `README.md`
2. 查看 `src/utils/request.doc.md` 了解网络请求
3. 提交 Issue：[GitHub Issues](https://github.com/your-username/create-yxuer-vue/issues)

## 🎉 开始开发吧！

现在您已经准备好开始开发了。祝您编码愉快！

---

**提示**：如果 VSCode 提示安装扩展，请允许安装以获得最佳开发体验。
