# 发布指南

本文档说明如何将 `create-yxuer-vue` 发布到 npm。

## 📋 发布前检查清单

### 1. 确保所有文件准备就绪

- [x] `package.json` - 版本号、描述、关键词等信息完整
- [x] `README.md` - 详细的使用文档
- [x] `.npmignore` - 排除不必要的文件
- [x] `template/` - 完整的项目模板
- [x] `bin/index.js` - CLI 入口文件（包含 shebang）
- [x] `src/index.js` - 主逻辑文件

### 2. 测试脚手架

在发布前，务必在本地测试脚手架是否正常工作：

```bash
# 在 create-yxuer-vue 目录下
pnpm dev

# 或指定项目名称测试
pnpm test
```

测试创建的项目：

```bash
cd test-project
pnpm install
pnpm dev:master
```

确认以下功能正常：
- ✅ 项目文件正确生成
- ✅ 依赖安装成功
- ✅ 开发服务器启动正常
- ✅ 构建打包成功
- ✅ 可选功能（富文本、上传）按预期工作

### 3. 更新版本号

使用 npm version 命令更新版本：

```bash
# 补丁版本（bug 修复）: 1.0.0 -> 1.0.1
npm version patch

# 次版本（新功能）: 1.0.0 -> 1.1.0
npm version minor

# 主版本（破坏性更新）: 1.0.0 -> 2.0.0
npm version major
```

或手动编辑 `package.json` 中的 `version` 字段。

## 🚀 发布步骤

### 首次发布

#### 1. 注册 npm 账号

如果还没有 npm 账号，访问 [https://www.npmjs.com/signup](https://www.npmjs.com/signup) 注册。

#### 2. 登录 npm

```bash
npm login
```

输入用户名、密码和邮箱。

#### 3. 检查包名是否可用

```bash
npm search create-yxuer-vue
```

如果包名已被占用，需要修改 `package.json` 中的 `name` 字段。

建议的命名方案：
- `create-yxuer-vue`（推荐）
- `@your-scope/create-yxuer-vue`（使用作用域）
- `create-yxuer-vue-app`

#### 4. 发布到 npm

```bash
# 在 create-yxuer-vue 目录下
npm publish
```

如果使用作用域包且想公开发布：

```bash
npm publish --access public
```

### 后续更新发布

1. 修改代码
2. 更新版本号（`npm version patch/minor/major`）
3. 提交 git 更改
4. 发布新版本

```bash
# 更新版本
npm version patch

# 提交到 git
git add .
git commit -m "chore: bump version to x.x.x"
git push

# 发布
npm publish
```

## 📦 发布后验证

### 1. 检查 npm 包页面

访问 `https://www.npmjs.com/package/create-yxuer-vue` 确认包已发布。

### 2. 测试安装

在新目录中测试：

```bash
# 使用 npm
npm create yxuer-vue@latest my-test-app

# 使用 pnpm
pnpm create yxuer-vue my-test-app

# 使用 yarn
yarn create yxuer-vue my-test-app
```

### 3. 验证功能

进入创建的项目，确认所有功能正常：

```bash
cd my-test-app
pnpm install
pnpm dev:master
```

## 🔄 版本管理策略

遵循 [语义化版本](https://semver.org/lang/zh-CN/)：

- **主版本号（Major）**：不兼容的 API 修改
  - 例如：删除功能、更改 CLI 参数、更改模板结构

- **次版本号（Minor）**：向下兼容的功能性新增
  - 例如：新增可选功能、新增配置选项

- **修订号（Patch）**：向下兼容的问题修正
  - 例如：bug 修复、文档更新、依赖更新

## 🏷️ 发布标签

npm 支持使用标签来标识不同的发布版本：

```bash
# 发布 beta 版本
npm version prerelease --preid=beta
npm publish --tag beta

# 发布 alpha 版本
npm version prerelease --preid=alpha
npm publish --tag alpha

# 安装特定标签的版本
npm create yxuer-vue@beta
```

## 📝 发布日志

建议在每次发布时更新 CHANGELOG.md：

```markdown
## [1.0.1] - 2024-12-24

### 修复
- 修复环境变量配置问题
- 修复依赖版本冲突

### 优化
- 优化错误提示信息
- 更新文档
```

## ⚠️ 注意事项

1. **不要发布敏感信息**
   - 检查 `.npmignore` 确保排除了敏感文件
   - 不要在代码中硬编码密钥、密码等

2. **测试充分**
   - 在不同操作系统测试（Windows、macOS、Linux）
   - 测试不同的 Node.js 版本
   - 测试不同的包管理器（npm、pnpm、yarn）

3. **文档完整**
   - README.md 应包含清晰的使用说明
   - 提供示例和常见问题解答
   - 保持文档与代码同步

4. **版本兼容性**
   - 明确 Node.js 版本要求
   - 考虑依赖包的版本兼容性
   - 避免使用过新的语法特性

5. **持续维护**
   - 及时响应 issue
   - 定期更新依赖
   - 修复已知 bug

## 🔗 相关链接

- [npm 官方文档](https://docs.npmjs.com/)
- [语义化版本规范](https://semver.org/lang/zh-CN/)
- [npm 包发布最佳实践](https://docs.npmjs.com/packages-and-modules/contributing-packages-to-the-registry)

## 🆘 常见问题

### Q: 发布失败，提示包名已存在？

A: 包名在 npm 上是唯一的。可以：
- 使用作用域包：`@your-username/create-yxuer-vue`
- 更改包名：`create-yxuer-vue-pro`

### Q: 如何撤销已发布的版本？

A: 使用 `npm unpublish`，但有限制：
```bash
# 只能撤销 72 小时内发布的版本
npm unpublish create-yxuer-vue@1.0.0

# 撤销整个包（慎用）
npm unpublish create-yxuer-vue --force
```

建议使用 `npm deprecate` 而不是 unpublish：
```bash
npm deprecate create-yxuer-vue@1.0.0 "此版本存在严重 bug，请使用 1.0.1"
```

### Q: 如何更新包的 README？

A: 修改 README.md 后重新发布即可，npm 会自动更新包页面。

### Q: 发布后多久能在 npm 上搜索到？

A: 通常几分钟内就能搜索到，但 CDN 缓存可能需要更长时间更新。

---

**祝发布顺利！** 🎉
